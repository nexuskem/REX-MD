# Environment secrets — NEVER commit these
.env

# WhatsApp session files (stored in MongoDB instead, but keep local fallback out of git)
# IMPORTANT: Only one deployment should ever run against a given MONGODB_URI at once.
session/
/session
auth_info_baileys/

# Temporary downloaded files
temp/

# Node modules
node_modules/

# pm2 logs
logs/

# OS generated files
.DS_Store
Thumbs.db

# Editor directories
.vscode/
.idea/

# Build artifacts
*.log
